»ерархи€ логов сервера. –одительские логи содержат сообщени€ потомков.  орневой лог отображает все сообщени€, которые генерирует сервер.

"server"
	"server-db" -- все запросы к Ѕƒ
	"server-server" -- работа с сетевыми соединени€ми, создание клиентских потоков
		"server-client" -- обработка сообщений клиентов (бизнес логика)
			"server-client-<UNP>" -- обработка сообщений дл€ одного клиента
	"server-teller" -- поток обработки транзакций
